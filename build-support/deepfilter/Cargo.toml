[workspace]
resolver = "2"
members = ["libDF"]

[profile.release]
debug = false
opt-level = 3
lto = "thin"

[profile.release-lto]
inherits = "release"
debug = false
lto = "fat"
strip = true
panic = "abort"
[patch.crates-io]
tract-linalg = { path = 'patches/tract-linalg' }
